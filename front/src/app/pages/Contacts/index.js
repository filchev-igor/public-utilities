import Navbar from '../../components/Navbar';

const Contacts = () => {
  return (`
    ${Navbar()}
    
    <div class="container-fluid">
      <div class="row">
      
      </div>
    </div>
  `);
};

export default Contacts;
