import Navbar from '../../components/Navbar';

const ContentAdding = () => {
  return (`
    ${Navbar()}
    
    <div class="container-fluid">
      <div class="row">
        
      </div>
    </div>
  `);
};

export default ContentAdding;
