import Navbar from '../../components/Navbar';

const Home = () => {
  return (`
    ${Navbar()}
    
    <div class="container-fluid">
      <div class="row">
        
      </div>
    </div>
  `);
};

export default Home;
